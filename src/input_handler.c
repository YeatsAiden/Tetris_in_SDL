#include <SDL2/SDL.h>
#include <SDL2/SDL_events.h>
#include <stdlib.h>

void poll_events(SDL_Event *event_list) {
  SDL_Event event;

  int i = 1
  SDL_Event *event_list = calloc(sizeof(SDL_Event) * i)
  while (SDL_PollEvent(&event)) {

    }
  }
}
